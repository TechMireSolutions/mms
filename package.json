{
  "name": "mms-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "dev": "turbo run dev --filter=mms-frontend --filter=mms-backend",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.9.18"
  }
}
