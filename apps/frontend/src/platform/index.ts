/** Platform (apex) domain — routes, pages, components, hooks, lib. */
export { default as ApexRoutes, ApexRoutesWithSuspense } from '@/platform/routes/ApexRoutes';
export { usePlatformAuth, PlatformAuthProvider } from '@/platform/lib/PlatformAuthContext';
export type { PlatformAuthContextType } from '@/platform/lib/PlatformAuthContext';
