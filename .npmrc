# pnpm workspace defaults — modern monorepo best practices.

# Keep exact versions when adding deps so the lockfile stays deterministic.
# (Only affects future `pnpm add`; does not change existing resolutions.)
save-exact=true

# Node >= 24 is required (see root package.json `engines`).
